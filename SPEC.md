# IOT SPEC

IOT is a french internet of things project decompose in 3 parts: embedded, worker, web

The embedded part is responsible to send temperature and location data to the worker.
The worker part is responsible to process and store the data.
The web part is responsible to aggregate and display the data.

